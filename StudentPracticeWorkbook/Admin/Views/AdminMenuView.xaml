<UserControl x:Class="Admin.Views.AdminMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <Label Content="Admin menu"></Label>
        <Button Margin="10" Width="75" Content="Wydziały" Command="{Binding NavigationCommand}" CommandParameter="Admin.Views.AdminDepartmentsView"/>
        <Button Margin="10" Width="75" Content="Firmy" Command="{Binding NavigationCommand}" CommandParameter="Admin.Views.AdminCompaniesView"/>
        <Button Margin="10" Width="75" Content="Dzienniki" Command="{Binding NavigationCommand}" CommandParameter="Admin.Views.AdminWorkbooksView"/>
        <Button Margin="10" Width="75" Content="Studenci" Command="{Binding NavigationCommand}" CommandParameter="Admin.Views.AdminStudentsView"/>
    </StackPanel>
</UserControl>
